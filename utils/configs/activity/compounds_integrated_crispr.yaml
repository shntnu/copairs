# Phenotypic activity analysis for compounds integrated with CRISPR
# Corresponds to: 0.1.2.phenotypic-activity-compounds-integrated-crispr.py

data:
  path: "../profiles/integrated/profiles_var_mad_int_featselect_harmony.parquet"
  metadata_regex: "^Metadata"

preprocessing:
  # Filter for specific sources that are part of the integrated dataset
  # These are the compound sources that can be integrated with CRISPR
  - type: filter
    query: "Metadata_Source in ['source_2', 'source_6', 'source_7', 'source_8', 'source_10', 'source_11']"
  
  # Remove features with NaN values
  - type: remove_nan_features
  
  # Filter out empty wells
  - type: filter
    query: "Metadata_JCP2022 != 'JCP2022_999999'"
  
  # Keep only treatments
  - type: filter
    query: "Metadata_control_type == 'trt'"

average_precision:
  params:
    pos_sameby: ["Metadata_JCP2022"]
    pos_diffby: []
    neg_sameby: []
    neg_diffby: ["Metadata_JCP2022"]
    batch_size: 20000
    distance: "cosine"

mean_average_precision:
  params:
    sameby: ["Metadata_JCP2022"]
    null_size: 20000
    threshold: 0.1
    seed: 12527

output:
  path: "output/phenotypic-activity-var_mad_int_featselect_harmony-integrated-crispr.csv.gz"
  save_ap_scores: false

plotting:
  enabled: false